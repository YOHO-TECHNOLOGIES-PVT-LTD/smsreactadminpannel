build:
  command: npm run build
  publish: dist
  environment:
    NODE_VERSION: "20"
    NPM_VERSION: "10"
    NPM_CONFIG_OPTIONAL: "false"


redirects:
  from = "/*"
  to = "/index.html"
  status = 200

